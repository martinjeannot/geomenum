/*
 * Copyright (c) 2014 Geomenum Inc. All Rights Reserved.
 */

package com.geomenum.core.misc.gui;

import org.testng.annotations.Test;

import static com.geomenum.config.TestingLevels.UNIT;

@Test(groups = {UNIT})
public class BreadcrumbNavigationHelperTest {

    /*public void buildBreadcrumbNavigationBar() {
        MenuNode root = MenuNodeFixtures.standardTree();
        MenuNode target = root.findNodeById(MenuNodeFixtures.LEAF_NODE2_DEPTH2_ID);

        List<Map<String, String>> navBarResult = breadcrumbNavigationHelper(null, target).buildBreadcrumbNavigationBar(Locale.ENGLISH);
        List<List<Map<String, String>>> dropdownContents = breadcrumbNavigationHelper(null, target).buildBreadcrumbDropdownContent(Locale.ENGLISH);
        assertEquals(1, 2);
    }*/
}
